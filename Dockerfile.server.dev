FROM node:22-slim

WORKDIR /app

COPY package*.json ./
COPY server/package*.json ./server/

RUN npm install --workspace=server

COPY server/ ./server/

EXPOSE 3001

# tsx watch gives us hot reload on src/ changes via volume mount
CMD ["npm", "run", "dev", "--workspace=server"]
